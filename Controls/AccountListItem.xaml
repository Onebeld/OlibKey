<UserControl x:Class="OlibKey.Controls.AccountListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
             Loaded="UserControl_Loaded" MinWidth="186"
             mc:Ignorable="d" MouseLeftButtonDown="UserControl_MouseDoubleClick">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Background="White" CornerRadius="999" Grid.RowSpan="2" Height="25" Width="25" Margin="5,0">
            <Image x:Name="imageIcon" Source="{Binding IconWebSite, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        </Border>
        <TextBlock Text="{Binding AccountName}" FontSize="16" Grid.Column="1"/>
        <TextBlock Text="{Binding Username}" FontSize="12" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</UserControl>
