<Window x:Class="OlibPasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        KeyDown="Window_KeyDown"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource Montserrat}"
        Title="Olib Password Manager" Height="450" Width="800" Background="#ecf0f5" MinWidth="600" MinHeight="350">
    <Window.Resources>
        <DataTemplate x:Key="ListPassword">
            <Grid Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Image Source="{Binding Path=Image}" Grid.RowSpan="2" Height="16"/>
                <TextBlock Text="{Binding Path=Name}" FontSize="16" Grid.Row="0" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=PasswordName}" Foreground="#777777" Grid.Row="1" Grid.Column="1"/>
            </Grid>
        </DataTemplate>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" SnapsToDevicePixels="true" StrokeThickness="1" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd" Background="White" SnapsToDevicePixels="true">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Bd2" Background="White"/>
                                <ContentPresenter Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="True" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="#FFE0E0E0"/>
                                <Setter Property="Background" TargetName="Bd2" Value="#FFE0E0E0"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="#FFE0E0E0"/>
                                <Setter Property="Background" TargetName="Bd2" Value="#931d60"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="#FFE0E0E0"/>
                                <Setter Property="Background" TargetName="Bd2" Value="#931d60"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="17"/>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Menu Grid.ColumnSpan="3" FontFamily="{StaticResource Montserrat}" BorderBrush="#dddddd" BorderThickness="0,0,0,1">
            <MenuItem Header="{DynamicResource File}">
                <MenuItem Header="{DynamicResource CreateBase}" Click="OpenCreateData" Template="{DynamicResource MenuItemTemplate1}"/>
                <MenuItem Header="{DynamicResource OpenBase}" Click="OpenBase" InputGestureText="Ctrl + O" Template="{DynamicResource MenuItemTemplate1}"/>
                <MenuItem Header="{DynamicResource SaveBase}" Click="SaveBase" InputGestureText="Ctrl + S" Template="{DynamicResource MenuItemTemplate1}"/>
                <Separator/>
                <MenuItem Header="{DynamicResource AddLogin}" Click="MenuItem_Click" InputGestureText="Ctrl + N" Template="{DynamicResource MenuItemTemplate1}"/>
                <Separator/>
                <MenuItem Header="{DynamicResource Settings}" Click="OpenSettingsWindow" Template="{DynamicResource MenuItemTemplate1}"/>
                <MenuItem Header="{DynamicResource ExitFrom}" InputGestureText="Alt + F4" Click="ClosedApplication" Template="{DynamicResource MenuItemTemplate1}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource Tools}">
                <MenuItem Header="{DynamicResource PasswordGenerator}" Click="OpenPasswordGeneratorWindow" InputGestureText="Ctrl + G" Template="{DynamicResource MenuItemTemplate1}"/>
                <MenuItem Header="{DynamicResource ChangeMasterPassword}" Click="OpenChangeMasterPassword" Template="{DynamicResource MenuItemTemplate1}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource Reference}">
                <MenuItem Header="{DynamicResource CheckUpdates}" Click="CheckUpdateButton" Template="{DynamicResource MenuItemTemplate1}"/>
                <MenuItem Header="{DynamicResource AboutPrograms}" Click="OpenAboutWindow" Template="{DynamicResource MenuItemTemplate1}"/>
            </MenuItem>
        </Menu>
        <ListBox x:Name="PasswordList" SelectedItem="SelectedUnit" BorderBrush="#dddddd" BorderThickness="1,0,1,0" Grid.Row="2" VirtualizingPanel.ScrollUnit="Pixel" Grid.Column="1" SelectionChanged="PasswordList_SelectionChanged" ItemTemplate="{StaticResource ListPassword}"/>
        <Border Grid.Column="1" Grid.Row="3" Background="#f9fafc" BorderBrush="#dddddd" BorderThickness="1" >
            <Button Content="{DynamicResource Add}" Margin="10" Click="CreatePassword"/>
        </Border>
        <Border Grid.Column="1" Grid.Row="1" Background="White" BorderBrush="#dddddd" BorderThickness="1,0,1,1">
            <DockPanel Margin="10">
                <Image Source="{DynamicResource loupeDrawingImage}" Height="20" Margin="0,0,5,0"/>
                <TextBox x:Name="TxtSearch" TextChanged="TxtSearch_TextChanged"/>
            </DockPanel>
        </Border>
        <Frame x:Name="FrameWindow" Grid.Column="2" Grid.Row="1" Grid.RowSpan="3" Source="/Pages/StartScreen.xaml" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
