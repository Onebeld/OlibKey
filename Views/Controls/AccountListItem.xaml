<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:l="clr-namespace:OlibKey.Core;assembly=OlibKey"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
			 Background="Transparent"
			 x:Class="OlibKey.Views.Controls.AccountListItem">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Border Background="White" CornerRadius="999" Grid.RowSpan="2" Height="25" Width="25" Margin="5,0">
			<Image x:Name="imageIconWebSite" Margin="5"/>
		</Border>
		<TextBlock x:Name="tbAccountName" FontWeight="Bold" FontSize="16" Grid.Row="0" Grid.Column="1" Foreground="{DynamicResource ThemeForegroundBrush}"/>
		<TextBlock x:Name="tbUsername" FontSize="12" Grid.Column="1" Grid.Row="1" Foreground="{DynamicResource ThemeForegroundOpacityBrush}"/>
	</Grid>
</UserControl>
