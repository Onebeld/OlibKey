<Window x:Class="OlibKey.Views.RequireMasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OlibKey.Views"
        mc:Ignorable="d"
        Title="RequireMasterPasswordWindow" Height="300" Width="400" Background="#FF5A0F26">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="1" Margin="10" VerticalAlignment="Top">
            <StackPanel>
                <Label Content="Тип" Foreground="Black"/>
                <ComboBox x:Name="cbType" Margin="5" SelectionChanged="cbType_SelectionChanged" SelectedIndex="{Binding TypeAccount, UpdateSourceTrigger=PropertyChanged}">
                    <ComboBoxItem Content="Логин"/>
                    <ComboBoxItem Content="Банковская карта"/>
                    <ComboBoxItem Content="Паспорт"/>
                </ComboBox>
            </StackPanel>
        </Border>
        <Border BorderBrush="Black" Background="White" Grid.Row="1" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button Content="Разблокировать" Click="Button_Click_1" Margin="15,0"/>
                <Button Content="Отмена" Click="Button_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
